meta {
  name: 3 - LOGIN - Correct password UNB MarketPlace
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  // {
  //   "email": "ada@example.com", "password": "12345678"
  // }
  
  {
    "email": "test@unb.ca", "password": "123456789"
  }
}

tests {
  const body = res.getBody();         
  bru.setEnvVar("token", body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
